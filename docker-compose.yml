services:
  web:
    build:
      context: .
      dockerfile: docker/web.Dockerfile
    ports:
      - "5173:5173"
    volumes:
      - ./web:/workspace/web
      - /workspace/web/node_modules
    environment:
      NODE_ENV: development
